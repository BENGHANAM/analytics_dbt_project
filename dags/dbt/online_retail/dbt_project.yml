name: online_retail
version: 1.0.0
config-version: 2

profile: online_retail

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  online_retail:
    staging:
      +materialized: table
      +schema: staging

    intermediate:
      +materialized: table
      +schema: staging

    mart:
      +materialized: table
      +schema: mart

      aggregations:
        +materialized: table
        +schema: mart

      kpi:
        +materialized: table
        +schema: mart